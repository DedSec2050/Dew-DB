# Azure Deployment Notes (Scaffold)

This folder is reserved for deployment manifests and runbooks targeting Azure.

Planned artifacts:

- `container-app.bicep` or `container-app.yaml` for Azure Container Apps
- AKS manifests/Helm chart for clustered deployments
- Storage configuration for AOF persistence via mounted volumes or Blob-backed flows
- Observability wiring with Azure Monitor and Application Insights
